/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable image domains for Google profile pictures
  images: {
    domains: ['lh3.googleusercontent.com'],
  },
  // Add any other configurations needed
};

export default nextConfig;
